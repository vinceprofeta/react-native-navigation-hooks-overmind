import * as textStyles from './text';
import * as colorStyles from './colors';
import * as layoutStyles from './layout';

export const text = textStyles;
export const colors = colorStyles;
export const layouts = layoutStyles;
